import io
import json
import sys


class JSONInventory:
    def write_file(
        self,
        file,
        playlist_tracks,
        playlist_info=None,
        fields=None,
        format=None,
    ):
        print(file)

    def stdout(
        self, playlist_tracks, playlist_info=None, fields=None, format=None
    ):
        print(playlist_tracks)
